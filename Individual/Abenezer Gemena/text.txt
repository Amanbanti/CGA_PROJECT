###Lesson 4: Shapes and Parameters
   The lesson explores how to use parameters to create customized and dynamic drawings in Game Lab.
   
   ##Overview
   
Guiding Question: How can parameters be used to provide the computer with more detailed instructions?

This lesson focuses on understanding and applying parameters to customize the characteristics of shapes. Students will learn how to adjust parameters to accurately control the placement and dimensions of shapes on the screen.

##Core Concepts

#Parameter:

Definition: Additional information provided to a function to customize its behavior.
Example: Adjusting the size, position, or color of a shape using parameters.
Introduced Code:

background(color): Sets the canvas background color.
ellipse(x, y, w, h): Draws an oval or circle with defined position and dimensions.
rect(x, y, w, h): Creates a rectangle with specified position and size.
##Levels
 #Predict
 predict how changing parameters will affect shapes.
 # Skill Building

   Practice using parameters to control shapes.
   Explore positioning, layering, and resizing.
   rect(150, 200, 100, 50); 
   ellipse(200, 150, 80, 80);
#Practice

 Combine multiple shapes to create a meaningful image, such as a face, tree, or car.
#Assessment

 Demonstrate understanding by completing challenges like drawing shapes in specific positions.
#Challenges

 Create dynamic and randomized designs.
 ellipse(randomNumber(0, 400), randomNumber(0, 400), 50, 50);

###Lesson 5 : Understanding Variables

##What Are Variables?

Variables allow you to store a single value in the computer's memory, giving it a descriptive name for easy reference. They are essential for managing and reusing values throughout your program or saving numbers for later use.

##Key Concepts
 
 #Creating Variables

Use the var command to create a new variable.
Example:
var size;
This creates a variable with the label size.
 
 #Assigning Values

Use the assignment operator = to assign a value to a variable.
Example:
size = 100;
This assigns the value 100 to size. Any previous value is replaced.
Read it as "size gets 100."
 #Using a Value

To use the variable's value, place its label where you want to use it in your code.
Example:
ellipse(200, 200, size, size);
Here, size is used to determine the ellipse's width and height.
Initializing a Variable

Variables are often given an initial value when created.
Combine var and = to initialize a variable in one step.
Example:
var size = 100;
This initializes the variable size with the value 100.
After initialization, use = to update the variable without var.



###Lesson 6: Random Numbers

  1. Using Random Numbers

The randomNumber() block generates random values within a specified range.
It requires two parameters: the minimum and maximum values for the generated number.
Example: randomNumber(1, 6) will produce a random number between 1 and 6, simulating a dice roll.
This block can be used anywhere a number is needed in your code, such as in calculations or drawing commands.

2. Drawing with Random Numbers

randomNumber() can be used as a parameter in drawing functions to create random visual outputs.
For example, you can use it to position shapes at random locations, randomize their sizes, or even change colors.
Each time the program runs, the output may look different depending on the random values generated.
Example:
ellipse(randomNumber(0, 400), randomNumber(0, 400), 50, 50);
This example places an ellipse at a random position on the screen.

3. Multiple Random Numbers

You can use randomNumber() multiple times in your program. Each call generates a new, independent random number.
Think of each randomNumber() as rolling a new die.
Example:
let x = randomNumber(0, 400);
let y = randomNumber(0, 400);
ellipse(x, y, 50, 50);
In this example, both the x and y positions of the ellipse are randomized.

# Lesson 7 : Mini-Project
  ## Review Regular Polygons
    regularPolygon(50, 75, 3, 50);
    regularPolygon(150, 75, 4, 50);
    regularPolygon(50, 175, 7, 50);
    regularPolygon(50, 275, 11, 50);
    //2) Add code to finish the drawing so it matches the picture
    regularPolygon(150,170,9,50);
    regularPolygon(150,275,11,50);
    regularPolygon(250,75,5,50);
    regularPolygon(250,175,11,50);
    regularPolygon(250,275,12,50);
    regularPolygon(350,75,6,50);
    regularPolygon(350,175,11,50);
    regularPolygon(350,275,12,50);
  ## Review the Shape Block
     fill("red");
     shape(200, 200, 0, 400, 400, 400);
     fill("blue");
     shape(200, 200, 0, 0, 400, 0);
    //2) Finish the code so it matches the picture
    fill("green");
    shape(0,0,200,200,0,400);
    fill("yellow");
    shape(400,0,200,200,400,400);
  ## Robot Face
    // Face (rectangle)
   regularPolygon(200, 200, 4, 150); // Head

  // Eyes (circles)
  regularPolygon(150, 150, 20, 30); // Left Eye
  regularPolygon(250, 150, 20, 30); // Right Eye

  // Nose (triangle)
   regularPolygon(200, 200, 3, 30); // Triangle for Nose

  // Mouth (ellipse)
  regularPolygon(200, 270, 20, 70); // Mouth as an elongated shape
# Lesson 8 : Sprites
     ##Investigating Sprites
     // Create the fruit sprite
var fruit = createSprite(200, 100); // Adjust position for vertical stacking
fruit.setAnimation("strawberry");// Set the fruit animation to "apple"

// Create the vegetable sprite
var vegetable = createSprite(200, 300); // Adjust position for vertical stacking
vegetable.setAnimation("broccoli");// Set the vegetable animation to "carrot"

// Draw the sprites
function draw() {
  background("white"); // Set a background color
  drawSprites(); // Display all sprites
}
## Sprites

// 1) Create a sprite variable
var student = createSprite(200, 200); // Create the student sprite at (200, 200)

// 2) Set the animation
student.setAnimation("Kim");// Replace "student" with the desired animation from the dropdown menu

// 3) Draw the sprite on the screen
function draw() {
  background("white"); // Set a background color
  drawSprites(); // Display all sprites on the screen
}
## Adding Animations

// Create the sprites
var topLeftSprite = createSprite(100, 100);
var topRightSprite = createSprite(300, 100);
var bottomLeftSprite = createSprite(100, 300);
var bottomRightSprite = createSprite(300, 300);

// Set animations for each sprite
topLeftSprite.setAnimation("alien");// Assign the "cat" animation
topRightSprite.setAnimation("bunny");// Assign the "dog" animation
bottomLeftSprite.setAnimation("flyBot");// Assign the "robot" animation
bottomRightSprite.setAnimation("planeRed1");// Assign the "alien" animation

// Draw the sprites
function draw() {
  background("white"); // Set a background color
  drawSprites();       // Render the sprites
}
## Debug some sprites
var drink = createSprite(100, 200);
drink.setAnimation("soda");

var dessert = createSprite(300, 200);
dessert.setAnimation("ice_cream");

var message = createSprite(200, 200);
message.setAnimation("textGetReady");

function draw() {
  background("white");
  drawSprites();
}
## Debug: Sprite Name


var sprite1 = createSprite(300, 200);
var sprite2 = createSprite(200, 275);
var sprite3 = createSprite(100, 200);

sprite1.setAnimation("cherry");
sprite2.setAnimation("cherry");
sprite3.setAnimation("cherry");

function draw() {
  background("white");
  drawSprites();
}


#Create your own sprites
var carSprite = createSprite(100, 200);
carSprite.setAnimation("car");

var treeSprite = createSprite(300, 200);
treeSprite.setAnimation("tree");

# Drawing your own image

function draw() {
  background("white");
  drawSprites();
}

var character = createSprite(200, 300);
character.setAnimation("myCharacter");

background("skyblue");

fill("green");
noStroke();
rect(0, 300, 400, 100);

drawSprites();

#Lesson 14 Mini-Project

  ###Create Your Scene - Sprites

  var ball = createSprite(200, 200);
  ball.setAnimation("soccerball_1");
  ball.scale = 0.13;
  ball.velocityX = 10;
  ball.velocityY = 5;
  ball.setCollider("circle");

  var p = createSprite(60, 360);
  p.setAnimation("animation_1");
  p.scale = 3;

  var edges = createEdgeSprites();

  function draw() {
    background("green");

    fill("blue");
    ellipse(100, 100, 50, 50);
    fill("yellow");
    rect(300, 50, 100, 50);

    if (keyDown("right")) {
      p.x = p.x + 10;
    }
    if (keyDown("left")) {
      p.x = p.x - 10;
    }

    ball.bounceOff(edges);
    ball.bounceOff(p);

    drawSprites();
  }

  #############Create Your Scene - Text#################

            var ball = createSprite(200, 200);
            ball.setAnimation("soccerball_1");
            ball.scale = 0.13;
            ball.velocityX = 10;
            ball.velocityY = 5;
            ball.setCollider("circle");

            var p = createSprite(60, 360);
            p.setAnimation("animation_1");
            p.scale = 3;

            var edges = createEdgeSprites();



            function draw() {
              background("green");
              fill("white");
              textSize(18)
              text("Start game",170,150);
              text("New Game",170,200);
              text("Resume",170,250);

              fill("blue");
              ellipse(100, 100, 50, 50);
              fill("yellow");
              rect(300, 50, 100, 50);

              if (keyDown("right")) {
                p.x = p.x + 10;
              }
              if (keyDown("left")) {
                p.x = p.x - 10;
              }

              ball.bounceOff(edges);
              ball.bounceOff(p);

              drawSprites();
            }
###########################################################




################Create Your Scene - Movement###############

            var ball = createSprite(200, 200);
            ball.setAnimation("soccerball_1");
            ball.scale = 0.13;
            ball.velocityX = 10;
            ball.velocityY = 5;
            ball.setCollider("circle");

            var p = createSprite(60, 360);
            p.setAnimation("animation_1");
            p.scale = 3;

            var edges = createEdgeSprites();

            var movingRectX = 300;
            var movingRectSpeed = 2;

            function draw() {
              background("green");

              fill("white");
              textSize(18);
              text("Start Game", 170, 150);
              text("New Game", 170, 200);
              text("Resume", 170, 250);

              fill("blue");
              ellipse(100, 100, 50, 50);
              fill("yellow");
              rect(movingRectX, 50, 100, 50);

              movingRectX += movingRectSpeed;
              if (movingRectX > 400 || movingRectX < 0) {
                movingRectSpeed *= -1;
              }

              if (frameCount % 60 === 0) {
                var randomX = random(50, 350);
                var randomY = random(50, 150);
                ellipse(randomX, randomY, 50, 50);
              }

              if (keyDown("right")) {
                p.x = p.x + 10;
              }
              if (keyDown("left")) {
                p.x = p.x - 10;
              }

              ball.bounceOff(edges);
              ball.bounceOff(p);

              drawSprites();
            }
##########################################################


                #Lesson 15 : Conditionals

#########################Booleans###########################
            var finishLine = createSprite(40, 200);
            finishLine.setAnimation("finish_line");

            var raceCar = createSprite(400, 200);
            raceCar.setAnimation("race_car");

            function draw() {
              background("white");
              
              // Counter pattern to make car move across the screen
              raceCar.x = raceCar.x - 2;
              
              // Program text
              fill("black");
              strokeWeight(5);
              textSize(25);
              text("The race car has crossed the finish line:", 100, 20, 300, 60);
              fill("blue");
              
              // Display boolean for whether the race car crossed the finish line
              text(raceCar.x < finishLine.x, 100, 100);
              
              // Draw Animations
              drawSprites();
            }

########################If Statement########################
          var finishLine = createSprite(40, 200);
          finishLine.setAnimation("finish_line");

          var raceCar = createSprite(400, 200);
          raceCar.setAnimation("race_car");

          function draw() {
            // Draw Background
            background("white");
            
            // Update values
            raceCar.x = raceCar.x - 2;
            if (raceCar.x < finishLine.x) {
              text("THE ORANGE CAR IS THE WINNER!",100,100);
            }
            // Draw Animations
            drawSprites();
          }
################################################################

############################Changing Fruit######################
                var fruit = createSprite(200, 200);
                fruit.setAnimation("apple");
                fruit.scale = 0.1;

                function draw() {
                  background("white");
                  
                  fruit.scale = fruit.scale + 0.01;
                  
                  if (fruit.scale > 2) {
                    fruit.setAnimation("pear");
                  }
                  
                  drawSprites();
                }

################################################################

#####################Boolean Expressions#########################

                var sprite1 = createSprite(160, 200);
                sprite1.setAnimation("apple");
                sprite1.rotation = 30;
                sprite1.scale = 0.5;

                var sprite2 = createSprite(300, 300);
                sprite2.setAnimation("carrot");
                sprite2.rotation = 30;
                sprite2.scale = 0.5;

                drawSprites();
                textSize(20);
                fill("blue");
                text("Comparison 1: " + (sprite2.x == sprite2.y), 0, 15);
                text("Comparison 2: " + (sprite2.rotation < 40), 0, 40);
                text("Comparison 3: " + (sprite1.scale == sprite2.scale), 0, 65);
                text("Comparison 4: " + (sprite1.x > 150), 0, 90);
                text("Comparison 5: " + (sprite2.y < 200), 0, 115);

##########################################################################

#############################Magic Dinosaur#################################

                  var backdrop = createSprite(200, 200);
                  backdrop.setAnimation("sci_fi");
                  var dinosaur = createSprite(200, 350);
                  dinosaur.scale = 0.2;
                  dinosaur.setAnimation("tyrannosaurus");

                  function draw() {
                    dinosaur.y = dinosaur.y - 5;

                    if (dinosaur.y < 100) {
                      dinosaur.setAnimation("pterodactyl");
                    }

                    drawSprites();
                  }
################################Restart Sprites##############################
                  var tree = createSprite(200, 220);
                  tree.setAnimation("tree");
                  tree.scale = 0.75;

                  var ghost = createSprite(300, 320);
                  ghost.setAnimation("ghost");

                  var monster = createSprite(400, 320);
                  monster.setAnimation("orange_monster");
                  monster.scale = 0.35;

                  function draw() {
                    background("plum");
                    fill("palegreen");
                    rect(-5, 350, 405, 50);

                    ghost.x = ghost.x - 2;
                    if (ghost.x < -50) {
                      ghost.x = 450;
                    }

                    monster.x = monster.x - 3;
                    if (monster.x < -50) {
                      monster.x = 450;
                    }

                    drawSprites();
                  }
######################################################################


                #Lesson 16 : Keyboard Input

#################################Changing Sprites######################

                    var sprite = createSprite(200, 200);
                    sprite.setAnimation("giraffe");

                    function draw() {
                      background("white");

                      if (keyDown("h")) {
                        sprite.setAnimation("hippo");
                      }

                      if (keyDown("p")) {
                        sprite.setAnimation("pig");
                      }

                      if (keyDown("r")) {
                        sprite.setAnimation("rabbit");
                      }

                      drawSprites();
                    }

#####################################################################



###################################Moving Sprites###########################
                    var bug = createSprite(200, 200);
                    bug.setAnimation("ladybug");

                    function draw() {
                      background("white");

                      if (keyDown("right")) {
                        bug.x = bug.x + 5; // Moves the sprite to the right
                      }

                      drawSprites();
                    }
######################################################################################

##########################Gears with Conditionals##############################
                  var blueGear = createSprite(100, 220);
                  blueGear.setAnimation("blue_gear");

                  var greenGear = createSprite(183, 298);
                  greenGear.setAnimation("green_gear");

                  var redGear = createSprite(185, 145);
                  redGear.setAnimation("red_gear");

                  function draw() {
                    background("white");

                    if (keyDown("space")) {
                      blueGear.rotation = blueGear.rotation - 1;
                      greenGear.rotation = greenGear.rotation + 1;
                      redGear.rotation = redGear.rotation + 1;
                    }

                    drawSprites();
                  }
###################################################################################

###################################Fish With Arrows################################
                var orangeFish = createSprite(400, randomNumber(0, 100));
                orangeFish.setAnimation("orange_fish");

                var blueFish = createSprite(250, randomNumber(0, 200));
                blueFish.setAnimation("blue_fish");

                var greenFish = createSprite(300, randomNumber(200, 300));
                greenFish.setAnimation("green_fish");

                var e1X = 10, e1Y = 400; 
                var e2X = 10, e2Y = 400; 
                var e3X = 10, e3Y = 400;

                function draw() {
                  background("navy");

                  if (keyDown("left")) {
                    orangeFish.x = orangeFish.x - 2; // Normal speed
                    blueFish.x = blueFish.x - 4;    // Faster speed
                    greenFish.x = greenFish.x - 1;  // Slower speed
                  }

                  e1X = e1X + 3;
                  e1Y = e1Y - 3;

                  e2X = e2X + 2;
                  e2Y = e2Y + 2;

                  e3X = e3X + 1;
                  e3Y = e3Y - 1;

                  fill("white");
                  ellipse(e1X, e1Y, 30, 30);
                  ellipse(e2X, e2Y, 30, 30);
                  ellipse(e3X, e3Y, 30, 30);

                  drawSprites();
                }
#########################################################################

####################################Move in All Directions###########################
                var backdrop = createSprite(200, 200);
                backdrop.setAnimation("rainbow");
                var flyer = createSprite(200, 200);
                flyer.setAnimation("wing_bot");

                function draw() {
                  background("white");

                  if (keyDown("left")) {
                    flyer.x = flyer.x - 2; // Move left
                  }
                  
                  if (keyDown("right")) {
                    flyer.x = flyer.x + 2; // Move right
                  }
                  
                  if (keyDown("up")) {
                    flyer.y = flyer.y - 2; // Move up
                  }
                  
                  if (keyDown("down")) {
                    flyer.y = flyer.y + 2; // Move down
                  }

                  drawSprites();
                }
###########################################################################


###############################Responding to a Single Click#################################
                  var clicks = 0;

                  function draw() {
                    if (keyWentDown("space")) {
                      clicks = clicks + 1; // Only increment for new key presses
                    }
                    
                    background("white");
                    textSize(50);
                    text(clicks, 165, 175, 70, 50);
                  }
############################################################################

                  #Lesson 17 : Mouse Input
#########################################Reverse the Gears##########################
                  var blueGear = createSprite(100, 220);
                  blueGear.setAnimation("blue_gear");

                  var greenGear = createSprite(183, 298);
                  greenGear.setAnimation("green_gear");

                  var redGear = createSprite(185, 145);
                  redGear.setAnimation("red_gear");

                  function draw() {
                    background("white");

                    if (keyDown("space")) {
                      blueGear.rotation = blueGear.rotation - 1;
                      greenGear.rotation = greenGear.rotation + 1;
                      redGear.rotation = redGear.rotation + 1;
                    } else {
                      blueGear.rotation = blueGear.rotation + 1;  // Reversed direction
                      greenGear.rotation = greenGear.rotation - 1;  // Reversed direction
                      redGear.rotation = redGear.rotation - 1;  // Reversed direction
                    }

                    drawSprites();
                  }

#########################################################################

####################################Control the Gears############################
                var blueGear = createSprite(100, 220);
                blueGear.setAnimation("blue_gear");

                var greenGear = createSprite(183, 298);
                greenGear.setAnimation("green_gear");

                var redGear = createSprite(185, 145);
                redGear.setAnimation("red_gear");

                function draw() {
                  background("white");
                  
                  if (mouseDown("leftButton")) {
                    blueGear.rotation = blueGear.rotation - 1;
                    greenGear.rotation = greenGear.rotation + 1;
                    redGear.rotation = redGear.rotation + 1;
                  } else {
                    blueGear.rotation = blueGear.rotation + 1;  // Reversed direction
                    greenGear.rotation = greenGear.rotation - 1;  // Reversed direction
                    redGear.rotation = redGear.rotation - 1;  // Reversed direction
                  }

                  drawSprites();
                }

#######################################################################


#############################Mouse Clicks################################
              var balloon = createSprite(200, 50);
              balloon.setAnimation("balloon");
              balloon.scale = 0.1;

              function draw() {
                background("white");

                // Check if the left mouse button is being pressed
                if (mouseDown("leftButton")) {
                  balloon.y = balloon.y - 1;  // Move the balloon up
                } else {
                  balloon.y = balloon.y + 1;  // Move the balloon down
                }

                drawSprites();
              }
###########################################################################

####################################World.mouseX#######################

                  var blender = createSprite(100, 300);
                  blender.setAnimation("blender");
                  var mixer = createSprite(300, 300);
                  mixer.setAnimation("mixer");

                  function draw() {
                    background("lightyellow");
                    fill("burlywood");
                    rect(0, 350, 400, 50);

                    if (World.mouseX < 200) {
                      blender.x = randomNumber(95, 105);
                      blender.y = randomNumber(295, 305);
                    } else {
                      mixer.x = randomNumber(295, 305);
                      mixer.y = randomNumber(295, 305);
                    }

                    drawSprites();
                  }
#################################################################################
#########################################Ladybug Walk############################
                  var outside = createSprite(200, 200);
                  outside.setAnimation("pine_trees");
                  var bug = createSprite(350, 350);
                  bug.setAnimation("ladybug");

                  function draw() {
                    background("white");

                    //1) Use a conditional to check the position of the mouse.
                    if (World.mouseY > 300) {  // Check if the mouse is near the bottom of the screen
                      //2) Add code to make the bug move only if the mouse pointer is near the bottom of the screen.
                      bug.x = bug.x + 2;  // Move the bug to the right
                    }

                    drawSprites();
                  }

#################################################################################
#####################################Fix the Program#########################
var backdrop = createSprite(200, 200);
backdrop.setAnimation("sky");
var creature = createSprite(200, 250);
creature.setAnimation("creature");
creature.scale = 0.2;

function draw() {
  background("white");

  //1) Change the code so that the creature only shakes when the mouse is pressed.
  if (mouseIsPressed) {
    creature.rotation = randomNumber(-5, 5); // Shake the creature when the mouse is pressed
  }

  drawSprites();

  //2) Change the code so the directions ("Press the mouse to shake the creature.") only show
  //when the user is NOT pressing the mouse.
  if (!mouseIsPressed) {
    fill("black");
    textSize(40);
    text("Press the mouse to shake the creature.", 20, 50, 360, 100);
  }
}

################################################################################

###############################Twin Spirals##############################

var spiral = createSprite(100, 200);
spiral.setAnimation("lollipop");
var spiral2 = createSprite(300, 200);
spiral2.setAnimation("lollipop2");

function draw() {
  background("pink");

  // Check if the mouse is pressed
  if (mouseIsPressed) {
    // Reverse the behavior when the mouse is pressed
    spiral.scale = spiral.scale * 1.01;
    spiral.rotation = spiral.rotation - 3;
    spiral2.scale = spiral2.scale / 1.01;
    spiral2.rotation = spiral2.rotation + 3;
  } else {
    // Default behavior (no mouse press)
    spiral.scale = spiral.scale / 1.01;
    spiral.rotation = spiral.rotation + 3;
    spiral2.scale = spiral2.scale * 1.01;
    spiral2.rotation = spiral2.rotation - 3;
  }

  drawSprites();
}
#################################################################

        #Lesson 18 : Project - interactive Card

#######################Laying Out Your Background###########
function draw() {
  background("skyblue");

  fill("green");
  rect(0, 300, 400, 100);

  fill("yellow");
  ellipse(350, 50, 80, 80);

  fill("white");
  ellipse(randomNumber(50, 100), 100, 60, 40);
  ellipse(200, 150, 70, 50);
  ellipse(randomNumber(300, 350), 80, 80, 60);

  fill("brown");
  rect(120, 220, 20, 80);

  fill("darkgreen");
  ellipse(130, 200, 100, 100);

  fill("red");
  rect(200, 220, 100, 80);

  fill("brown");
  triangle(200, 220, 250, 170, 300, 220);
}
#################################################################

#########################Adding Sprites###############################
            var bird = createSprite(50, 150);
            bird.setAnimation("birdside_05_1");
            bird.scale = 0.5;

            var butterfly = createSprite(300, 100);
            butterfly.setAnimation("birdside_05_1");
            butterfly.scale = 0.5;

            function draw() {
              background("skyblue");
              
              fill("green");
              rect(0, 300, 400, 100);
              
              fill("yellow");
              ellipse(350, 50, 80, 80);
              
              fill("white");
              ellipse(randomNumber(50, 100), 100, 60, 40);
              ellipse(200, 150, 70, 50);
              ellipse(randomNumber(300, 350), 80, 60);
              
              fill("brown");
              rect(120, 220, 20, 80);
              
              fill("darkgreen");
              ellipse(130, 200, 100, 100);
              
              fill("red");
              rect(200, 220, 100, 80);
              
              fill("brown");
              triangle(200, 220, 250, 170, 300, 220);
              
              bird.x += 2;
              if (bird.x > 400) {
                bird.x = 0;
              }
              
              butterfly.y += randomNumber(-2, 2);
              butterfly.x += -1;
              if (butterfly.x < 0) {
                butterfly.x = 400;
              }
              
              drawSprites();
            }

###############################################################

#############################User Input###################################

                  var bird = createSprite(50, 150);
                  bird.setAnimation("birdside_05_1");
                  bird.scale = 0.5;

                  var butterfly = createSprite(300, 100);
                  butterfly.setAnimation("birdside_05_1");
                  butterfly.scale = 0.5;

                  function draw() {
                    background("skyblue");
                    
                    fill("green");
                    rect(0, 300, 400, 100);
                    
                    fill("yellow");
                    ellipse(350, 50, 80, 80);
                    
                    fill("white");
                    ellipse(randomNumber(50, 100), 100, 60, 40);
                    ellipse(200, 150, 70, 50);
                    ellipse(randomNumber(300, 350), 80, 60);
                    
                    fill("brown");
                    rect(120, 220, 20, 80);
                    
                    fill("darkgreen");
                    ellipse(130, 200, 100, 100);
                    
                    fill("red");
                    rect(200, 220, 100, 80);
                    
                    fill("brown");
                    triangle(200, 220, 250, 170, 300, 220);
                    
                    bird.x += 2;
                    if (bird.x > 400) {
                      bird.x = 0;
                    }
                    
                    butterfly.y += randomNumber(-2, 2);
                    butterfly.x += -1;
                    if (butterfly.x < 0) {
                      butterfly.x = 400;
                    }
                    
                    if (keyDown("RIGHT_ARROW")) {
                      bird.x += 5;
                    }
                    
                    if (keyDown("LEFT_ARROW")) {
                      bird.x -= 5;
                    }
                    
                    if (mouseDown("leftButton")) {
                      butterfly.scale += 0.01;
                    } else if (mouseDown("rightButton")) {
                      butterfly.scale -= 0.01;
                    }
                    
                    if (keyDown("UP_ARROW") && butterfly.scale > 0.3) {
                      butterfly.scale -= 0.01;
                    }
                    
                    if (keyDown("DOWN_ARROW") && butterfly.scale < 1) {
                      butterfly.scale += 0.01;
                    }
                    
                    drawSprites();
                  }
####################################################################


##############################Other Conditionals#####################

                  var bird = createSprite(50, 150);
                  bird.setAnimation("birdside_05_1");
                  bird.scale = 0.5;

                  var butterfly = createSprite(300, 100);
                  butterfly.setAnimation("birdside_05_1");
                  butterfly.scale = 0.5;

                  var birdSpeed = 2;
                  var butterflyDirection = randomNumber(-2, 2);

                  function draw() {
                    background("skyblue");
                    
                    fill("green");
                    rect(0, 300, 400, 100);
                    
                    fill("yellow");
                    ellipse(350, 50, 80, 80);
                    
                    fill("white");
                    ellipse(randomNumber(50, 100), 100, 60, 40);
                    ellipse(200, 150, 70, 50);
                    ellipse(randomNumber(300, 350), 80, 60);
                    
                    fill("brown");
                    rect(120, 220, 20, 80);
                    
                    fill("darkgreen");
                    ellipse(130, 200, 100, 100);
                    
                    fill("red");
                    rect(200, 220, 100, 80);
                    
                    fill("brown");
                    triangle(200, 220, 250, 170, 300, 220);
                    
                    bird.x += birdSpeed;
                    if (bird.x > 400) {
                      bird.x = 0;
                    }
                    
                    butterfly.y += butterflyDirection;
                    butterfly.x += -1;
                    if (butterfly.x < 0) {
                      butterfly.x = 400;
                    }

                    if (bird.x > 200 && bird.x < 300) {
                      birdSpeed = 5;
                    } else {
                      birdSpeed = 2;
                    }

                    if (butterfly.y < 100) {
                      butterflyDirection = randomNumber(2, 5); 
                    } else if (butterfly.y > 300) {
                      butterflyDirection = randomNumber(-5, -2); 
                    }

                    if (keyDown("RIGHT_ARROW")) {
                      bird.x += 5;
                    }

                    if (keyDown("LEFT_ARROW")) {
                      bird.x -= 5;
                    }

                    if (mouseDown("leftButton")) {
                      butterfly.scale += 0.01;
                    } else if (mouseDown("rightButton")) {
                      butterfly.scale -= 0.01;
                    }
                    
                    if (keyDown("UP_ARROW") && butterfly.scale > 0.3) {
                      butterfly.scale -= 0.01;
                    }
                    
                    if (keyDown("DOWN_ARROW") && butterfly.scale < 1) {
                      butterfly.scale += 0.01;
                    }
                    
                    drawSprites();
                  }
#####################################################################

                #Chapter 2 : Building Games
    ## Lesson 19 : Velocity
#########################velocityX####################################
            var fish = createSprite(50, 150);
            fish.setAnimation("orange_fish");
            fish.velocityX = 1; // This makes the fish move

            function draw() {
              // draw the background
              background(rgb(0,50,250));
              fill(rgb(250,250,250, 80));
              noStroke();
              ellipse(60, 20, 50, 50);
              ellipse(150, 100, 50, 50);
              ellipse(300, 130, 50, 50);
              ellipse(250, 200, 50, 50);
              ellipse(20, 250, 50, 50);
              ellipse(370, 330, 50, 50);
              fill(rgb(219, 208, 125));
              noStroke();
              ellipse(100, 400, 300, 100);
              ellipse(300, 400, 300, 75);
              
              drawSprites();
            }
#############################################################################

##############################Moving Down######################################
              var feather = createSprite(200, 50);
              feather.setAnimation("feather");
              feather.velocityY = 1; // This makes the feather move down

              function draw() {
                background("skyblue");
                drawSprites();
              }
################################################################################

#################################rotationSpeed##################################
            var sun = createSprite(200, 200);
            sun.setAnimation("sun");
            sun.rotationSpeed = 3; // This makes the sun rotate by 3 degrees each frame

            function draw() {
              background("blue");
              drawSprites();
            }
##############################################################################

###########################Controlling Speed#########################
var wheel = createSprite(200, 200);
wheel.setAnimation("wheel");

function draw() {
  background("blue");

  if (keyWentDown("space")) {
    wheel.rotationSpeed = 5; // This starts spinning the wheel when the space bar is pressed
  }

  drawSprites();
}
############################################################################
##########################Back and Forth###################################

var field = createSprite(200, 200);
field.setAnimation("field");
var ball = createSprite(200, 50);
ball.setAnimation("ball");
ball.scale = 0.2;
ball.velocityY = 5;

function draw() {
  if (ball.y > 380) {
    ball.velocityY = -5; // Bounces when it hits the bottom
  }

  // Check if the ball reaches the top of the screen and reverse its direction
  if (ball.y < 20) {
    ball.velocityY = 5; // Bounces when it hits the top
  }

  drawSprites();
}
################################################################################

###########################Controlling Speed################################
var flyBot = createSprite(200, 350);
flyBot.setAnimation("flyBot");

var isSpacePressed = false;

function draw() {
  background("skyblue");

  // Check if the space bar is pressed
  if (keyWentDown("space")) {
    isSpacePressed = true;
  }

  // Make the sprite move up if the space bar was pressed
  if (isSpacePressed) {
    flyBot.velocityY = -5; // Move up
  }

  drawSprites();
}
##########################################################################


#####################################Changing Course##################################
var alien = createSprite(50, 200);
alien.setAnimation("alien");
alien.velocityX = 0;
alien.velocityY = -3;

function draw() {

  if (alien.y < 50) {
    alien.velocityY = 0;
    alien.velocityX = 3;
  }

  if (alien.x > 350) {
    alien.velocityX = 0;
    alien.velocityY = 3;
  }

  if (alien.y > 350) {
    alien.velocityY = 0;
    alien.velocityX = -3;
  }

  if (alien.x < 50) {
    alien.velocityX = 0;
    alien.velocityY = -3;
  }

  drawSprites();
}

var space = createSprite(200, 200);
space.setAnimation("space");

var flag1 = createSprite(50, 50);
flag1.setAnimation("yellow_flag");

var flag2 = createSprite(350, 50);
flag2.setAnimation("yellow_flag");

var flag3 = createSprite(350, 350);
flag3.setAnimation("yellow_flag");

var flag4 = createSprite(50, 350);
flag4.setAnimation("yellow_flag");

alien.depth = 7;
######################################################################

              #Lesson 26 : The Game Design Process
###########################################Getting Started: Set Animations######################
var score = 0;

var cake = createSprite(350, 200);
cake.setAnimation("cake_animation");

var player = createSprite(200, 200);
player.setAnimation("player_animation");

var enemy1 = createSprite(100, 100);
enemy1.setAnimation("enemy1_animation");

var enemy2 = createSprite(300, 100);
enemy2.setAnimation("enemy2_animation");

function draw() {
  gameBackground();
  enemiesTouchCake();
  movePlayer();
  displaceEnemies();
  enemiesTouchWater();
  showScore();
  drawSprites();
}

function gameBackground() {
  noStroke();
  background(rgb(0, 100, 255));
  fill(rgb(100, 100, 100));
  rect(0, 150, 400, 100);
  fill(rgb(80, 80, 80));
  rect(0, 140, 400, 10);
  rect(0, 250, 400, 10);
}

function enemiesTouchCake() {
  if (player.isTouching(cake)) {
    score += 1;
    cake.x = random(50, 350); 
    cake.y = random(50, 150); 
  }
}

function movePlayer() {
  if (keyDown("left")) {
    player.x = player.x - 5;
  }

  if (keyDown("right")) {
    player.x = player.x + 5;
  }

  if (keyDown("up")) {
    player.y = player.y - 5;
  }

  if (keyDown("down")) {
    player.y = player.y + 5;
  }
}

function displaceEnemies() {
  if (player.isTouching(enemy1)) {
    enemy1.x = random(50, 350);
    enemy1.y = random(50, 150);
  }
  
  if (player.isTouching(enemy2)) {
    enemy2.x = random(50, 350);
    enemy2.y = random(50, 150);
  }
}

function enemiesTouchWater() {
  if (enemy1.isTouching(cake) || enemy2.isTouching(cake)) {
    score -= 1;
    enemy1.x = random(50, 350);
    enemy1.y = random(50, 150);
    enemy2.x = random(50, 350);
    enemy2.y = random(50, 150);
  }
}

function showScore() {
  fill("white");
  textSize(20);
  text("Score", 20, 20, 200, 100);
  text(score, 20, 40, 200, 100);
}
#######################################################################


#################################Moving The Enemies###############################
var score = 0;

var cake = createSprite(350, 200);
cake.setAnimation("face_birthdaycake_1");

var player = createSprite(200, 200);
player.setAnimation("rpgcharacter_02_1");

var enemy1 = createSprite(100, 100);
enemy1.setAnimation("enemyBlack1_1");

var enemy2 = createSprite(300, 100);
enemy2.setAnimation("enemyBlack1_1");

function draw() {
  gameBackground();
  enemiesTouchCake();
  movePlayer();
  displaceEnemies();
  enemiesTouchWater();
  showScore();
  drawSprites();
}

function gameBackground() {
  noStroke();
  background(rgb(0, 100, 255));
  fill(rgb(100, 100, 100));
  rect(0, 150, 400, 100);
  fill(rgb(80, 80, 80));
  rect(0, 140, 400, 10);
  rect(0, 250, 400, 10);
}

function enemiesTouchCake() {
  if (player.isTouching(cake)) {
    score += 1;
    cake.x = random(50, 350); 
    cake.y = random(50, 150); 
  }
}

function movePlayer() {
  if (keyDown("left")) {
    player.x = player.x - 5;
  }

  if (keyDown("right")) {
    player.x = player.x + 5;
  }

  if (keyDown("up")) {
    player.y = player.y - 5;
  }

  if (keyDown("down")) {
    player.y = player.y + 5;
  }
}

function displaceEnemies() {
  if (player.isTouching(enemy1)) {
    enemy1.x = random(50, 350);
    enemy1.y = random(50, 150);
  }
  
  if (player.isTouching(enemy2)) {
    enemy2.x = random(50, 350);
    enemy2.y = random(50, 150);
  }
}

function enemiesTouchWater() {
  if (enemy1.isTouching(cake) || enemy2.isTouching(cake)) {
    score -= 1;
    enemy1.x = random(50, 350);
    enemy1.y = random(50, 150);
    enemy2.x = random(50, 350);
    enemy2.y = random(50, 150);
  }
}

function showScore() {
  fill("white");
  textSize(20);
  text("Score", 20, 20, 200, 100);
  text(score, 20, 40, 200, 100);
}
##############################################################################

###############################  Moving Left and Right  ##########################################################
                      var score = 0;

                      var cake = createSprite(350, 200);
                      cake.setAnimation("face_birthdaycake_1");

                      var player = createSprite(200, 200);
                      player.setAnimation("rpgcharacter_02_1");

                      var enemy1 = createSprite(100, 100);
                      enemy1.setAnimation("enemyBlack1_1");

                      var enemy2 = createSprite(300, 100);
                      enemy2.setAnimation("enemyBlack1_1");

                      function draw() {
                        gameBackground();
                        enemiesTouchCake();
                        movePlayer();
                        displaceEnemies();
                        enemiesTouchWater();
                        showScore();
                        drawSprites();
                      }

                      function gameBackground() {
                        noStroke();
                        background(rgb(0, 100, 255));
                        fill(rgb(100, 100, 100));
                        rect(0, 150, 400, 100);
                        fill(rgb(80, 80, 80));
                        rect(0, 140, 400, 10);
                        rect(0, 250, 400, 10);
                      }

                      function enemiesTouchCake() {
                        if (player.isTouching(cake)) {
                          score += 1;
                          cake.x = random(50, 350); 
                          cake.y = random(50, 150); 
                        }
                      }

                      function movePlayer() {
                        if (keyDown("left")) {
                          player.x = player.x - 3;  // Move player left
                          player.setAnimation("rpgcharacter_left");  // Set animation to left
                        }

                        if (keyDown("right")) {
                          player.x = player.x + 3;  // Move player right
                          player.setAnimation("rpgcharacter_right");  // Set animation to right
                        }

                        if (keyDown("up")) {
                          player.y = player.y - 3;
                        }

                        if (keyDown("down")) {
                          player.y = player.y + 3;
                        }
                      }

                      function displaceEnemies() {
                        if (player.isTouching(enemy1)) {
                          enemy1.x = random(50, 350);
                          enemy1.y = random(50, 150);
                        }
                        
                        if (player.isTouching(enemy2)) {
                          enemy2.x = random(50, 350);
                          enemy2.y = random(50, 150);
                        }
                      }

                      function enemiesTouchWater() {
                        if (enemy1.isTouching(cake) || enemy2.isTouching(cake)) {
                          score -= 1;
                          enemy1.x = random(50, 350);
                          enemy1.y = random(50, 150);
                          enemy2.x = random(50, 350);
                          enemy2.y = random(50, 150);
                        }
                      }

                      function showScore() {
                        fill("white");
                        textSize(20);
                        text("Score", 20, 20, 200, 100);
                        text(score, 20, 40, 200, 100);
                      }
###########################################################################################################


